const DUMMY_DATA = [1, 2, 3, 4, 5];
const DUMMY_OPTIONS = {
  height: 3,
  width: 5
}
const DUMMY_ELEMENT = $('#chart');
/*
  Parameters:
  data - the data the chart will work with
  options - an object that has options for the chart, such as width and height
  element - the DOM element that the chart will be rendered into
*/
const drawBarChart = (data, options, element) => {
  console.log(data, options, element);
};

// document ready
$(() => {

})
